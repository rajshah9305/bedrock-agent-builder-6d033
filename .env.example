# AWS Configuration
VITE_AWS_REGION=us-east-1
VITE_AWS_ACCOUNT_ID=your-account-id

# Backend API Configuration
VITE_API_BASE_URL=http://localhost:3000/api
# For production, use your deployed API URL:
# VITE_API_BASE_URL=https://your-api.vercel.app/api

# Optional: Application Configuration
VITE_APP_NAME=AWS Bedrock Agent Builder
VITE_APP_VERSION=1.0.0

# Note: NEVER put AWS credentials in frontend .env files!
# AWS credentials should only be in your backend/API server
